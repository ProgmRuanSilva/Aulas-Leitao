//obcevarção: codigo não executavel!!

// Procedural gouTo era uma linguagem não estruturada

// Paradigma Procedural: Ele é baseado no Procedimento, vc declara e depois vc pode usar o mesmo procedimento varias vezes
processamento(valor1, valor2, valor3)
// vc tem uma função que manipula os dados

// OO: vc agora vc tem dados e dentro desses dados vc tem funções 
Objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        //...
    }
}

Objeto.processamento() // o foco passou a ser o objeto
// o objeto tem essas "Capsulas"  
//a ideia é mapear o mundo simplificado para o algoritimo
/*

Principios Importantes:

    1. Abstração :Exemplo do carro
    2. Encapsulamento : Exemplo mudar só interface
    3. Herança [Prototype] : Da Pra Reutilizar os Atributos a Partir de objetos Simples (é um:)
    Priorize a composição do que a Herança.
    4. Polimorfismo : vc pode abstrair sob uma variavel 

*/

